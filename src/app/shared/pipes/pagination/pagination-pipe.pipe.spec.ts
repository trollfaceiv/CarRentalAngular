import { PaginationPipePipe } from './pagination-pipe.pipe';

describe('PaginationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
